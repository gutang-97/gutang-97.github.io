# Gu Tang Academic Homepage

Static GitHub Pages homepage for Gu Tang.

## Local preview

```bash
python3 -m http.server 8000
```

Then open `http://localhost:8000`.

## Publish

Create a public repository named `gutang-97.github.io` under the `gutang-97`
GitHub account, then push the contents of this folder to the repository's
default branch.
